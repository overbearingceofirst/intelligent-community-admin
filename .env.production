# 页面标题
VITE_APP_TITLE = 智能社区管理系统

# 生产环境配置
VITE_APP_ENV = 'production'

# 智能社区后端 API（生产环境请将 /api 替换为真实后端地址或 nginx 反代路径）
# 如果 nginx 将 /api 代理到 Node 后端，保持 /api 即可
VITE_APP_BASE_API = /api

# 是否在打包时开启压缩，支持 gzip 和 brotli
VITE_BUILD_COMPRESS = gzip